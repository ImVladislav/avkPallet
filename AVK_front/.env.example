# Обов’язково на Vercel (Environment Variables), якщо API не на тому ж домені що й фронт.
# Якщо не задати — у збірці використається резервний URL бекенду з коду (apiUrl.ts).
VITE_API_BASE_URL=https://avkpallet-back.onrender.com
